# Tracked Integration Specs

These files are maintained with the integration source:

- `provider_specs.json`
- `template_specs.json`

They contain the authored canonical-provider and rich-template rules used by
the runtime after local slot metadata has been generated in `../data/`.
